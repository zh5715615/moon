<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="tcbv.zhaohui.moon.dao.BullfightingRecordDao">

        <resultMap type="tcbv.zhaohui.moon.entity.BullfightingRecordEntity" id="BullfightingRecordMap">
            <result property="id" column="id" jdbcType="VARCHAR"/>
            <result property="score" column="score" jdbcType="INTEGER"/>
            <result property="userId" column="user_id" jdbcType="VARCHAR"/>
            <result property="winlose" column="winlose" jdbcType="INTEGER"/>
            <result property="selfCard" column="self_card" jdbcType="VARCHAR"/>
            <result property="otherCard" column="other_card" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        </resultMap>


        <!--查询单个-->
        <select id="queryById" resultMap="BullfightingRecordMap">
            select id, score, user_id, winlose, self_card, other_card, create_time
            from tb_bullfighting_record
            where id = #{id}
        </select>

        <!--查询指定行数据-->
        <select id="queryAllByLimit" resultMap="BullfightingRecordMap">
            select  id, score, user_id, winlose, self_card, other_card, create_time
            from tb_bullfighting_record
            <where>
                <if test="entity.id != null and entity.id != ''">
                    and id = #{entity.id}
                </if>
                <if test="entity.score != null">
                    and score = #{entity.score}
                </if>
                <if test="entity.userId != null and entity.userId != ''">
                    and user_id = #{entity.userId}
                </if>
                <if test="entity.winlose != null">
                    and winlose = #{entity.winlose}
                </if>
                <if test="entity.selfCard != null and entity.selfCard != ''">
                    and self_card = #{entity.selfCard}
                </if>
                <if test="entity.otherCard != null and entity.otherCard != ''">
                    and other_card = #{entity.otherCard}
                </if>
                <if test="entity.createTime != null">
                    and create_time = #{entity.createTime}
                </if>
            </where>
            <if test="pageable.sort != null">
                ORDER BY
                <foreach collection="pageable.sort" item="order" separator=",">
                    ${order.property} ${order.direction}
                </foreach>
            </if>
            limit #{pageable.offset}, #{pageable.pageSize}
        </select>

        <!--统计总行数-->
        <select id="count" resultType="java.lang.Long">
            select count(1)
            from tb_bullfighting_record
            <where>
                <if test="id != null and id != ''">
                    and id = #{id}
                </if>
                <if test="score != null">
                    and score = #{score}
                </if>
                <if test="userId != null and userId != ''">
                    and user_id = #{userId}
                </if>
                <if test="winlose != null">
                    and winlose = #{winlose}
                </if>
                <if test="selfCard != null and selfCard != ''">
                    and self_card = #{selfCard}
                </if>
                <if test="otherCard != null and otherCard != ''">
                    and other_card = #{otherCard}
                </if>
                <if test="createTime != null">
                    and create_time = #{createTime}
                </if>
            </where>
        </select>

        <!--新增所有列-->
        <insert id="insert" keyProperty="id" useGeneratedKeys="false">
            insert into tb_bullfighting_record(id, score, user_id, winlose, self_card, other_card, create_time)
            values (#{id}, #{score}, #{userId}, #{winlose}, #{selfCard}, #{otherCard}, #{createTime})
        </insert>

        <insert id="insertBatch" keyProperty="id" useGeneratedKeys="false">
            insert into tb_bullfighting_record(id, score, user_id, winlose, self_card, other_card, create_time)
            values
            <foreach collection="entities" item="entity" separator=",">
                (#{entity.id}, #{entity.score}, #{entity.userId}, #{entity.winlose}, #{entity.selfCard}, #{entity.otherCard}, #{entity.createTime})
            </foreach>
        </insert>

        <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
            insert into tb_bullfighting_record(id, score, user_id, winlose, self_card, other_card, create_time)
            values
            <foreach collection="entities" item="entity" separator=",">
                (#{entity.id}, #{entity.score}, #{entity.userId}, #{entity.winlose}, #{entity.selfCard}, #{entity.otherCard}, #{entity.createTime})
            </foreach>
            on duplicate key update
                id = values(id),
                score = values(score),
                user_id = values(user_id),
                winlose = values(winlose),
                self_card = values(self_card),
                other_card = values(other_card),
                create_time = values(create_time)
        </insert>

        <!--通过主键修改数据-->
        <update id="update">
            update tb_bullfighting_record
            <set>
                <if test="score != null">
                    score = #{score},
                </if>
                <if test="userId != null and userId != ''">
                    user_id = #{userId},
                </if>
                <if test="winlose != null">
                    winlose = #{winlose},
                </if>
                <if test="selfCard != null and selfCard != ''">
                    self_card = #{selfCard},
                </if>
                <if test="otherCard != null and otherCard != ''">
                    other_card = #{otherCard},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime},
                </if>
            </set>
            where id = #{id}
        </update>

        <!--通过主键删除-->
        <delete id="deleteById">
            delete from tb_bullfighting_record where id = #{id}
        </delete>

    </mapper>
