<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="tcbv.zhaohui.moon.dao.BullfightingPurchaseDao">

        <resultMap type="tcbv.zhaohui.moon.entity.BullfightingPurchaseEntity" id="BullfightingPurchaseMap">
            <result property="id" column="id" jdbcType="VARCHAR"/>
            <result property="amount" column="amount" jdbcType="NUMERIC"/>
            <result property="times" column="times" jdbcType="INTEGER"/>
            <result property="userId" column="user_id" jdbcType="VARCHAR"/>
            <result property="address" column="address" jdbcType="VARCHAR"/>
            <result property="hash" column="hash" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        </resultMap>


        <!--查询单个-->
        <select id="queryById" resultMap="BullfightingPurchaseMap">
            select id, amount, times, user_id, address, hash, create_time
            from tb_bullfighting_purchase
            where id = #{id}
        </select>

        <!--查询指定行数据-->
        <select id="queryAllByLimit" resultMap="BullfightingPurchaseMap">
            select  id, amount, times, user_id, address, hash, create_time
            from tb_bullfighting_purchase
            <where>
                <if test="entity.id != null and entity.id != ''">
                    and id = #{entity.id}
                </if>
                <if test="entity.amount != null">
                    and amount = #{entity.amount}
                </if>
                <if test="entity.times != null">
                    and times = #{entity.times}
                </if>
                <if test="entity.userId != null and entity.userId != ''">
                    and user_id = #{entity.userId}
                </if>
                <if test="entity.address != null and entity.address != ''">
                    and address = #{entity.address}
                </if>
                <if test="entity.hash != null and entity.hash != ''">
                    and hash = #{entity.hash}
                </if>
                <if test="entity.createTime != null">
                    and create_time = #{entity.createTime}
                </if>
            </where>
            <if test="pageable.sort != null">
                ORDER BY
                <foreach collection="pageable.sort" item="order" separator=",">
                    ${order.property} ${order.direction}
                </foreach>
            </if>
            limit #{pageable.offset}, #{pageable.pageSize}
        </select>

        <!--统计总行数-->
        <select id="count" resultType="java.lang.Long">
            select count(1)
            from tb_bullfighting_purchase
            <where>
                <if test="id != null and id != ''">
                    and id = #{id}
                </if>
                <if test="amount != null">
                    and amount = #{amount}
                </if>
                <if test="times != null">
                    and times = #{times}
                </if>
                <if test="userId != null and userId != ''">
                    and user_id = #{userId}
                </if>
                <if test="address != null and address != ''">
                    and address = #{address}
                </if>
                <if test="hash != null and hash != ''">
                    and hash = #{hash}
                </if>
                <if test="createTime != null">
                    and create_time = #{createTime}
                </if>
            </where>
        </select>
    <select id="queryByHash" resultMap="BullfightingPurchaseMap">
        select  id, amount, times, user_id, address, hash, create_time
        from tb_bullfighting_purchase
        where hash = #{hash}
    </select>

    <!--新增所有列-->
        <insert id="insert" keyProperty="id" useGeneratedKeys="false">
            insert into tb_bullfighting_purchase(id, amount, times, user_id, address, hash, create_time)
            values (#{id}, #{amount}, #{times}, #{userId}, #{address}, #{hash}, #{createTime})
        </insert>

        <insert id="insertBatch" keyProperty="id" useGeneratedKeys="false">
            insert into tb_bullfighting_purchase(id, amount, times, user_id, address, hash, create_time)
            values
            <foreach collection="entities" item="entity" separator=",">
                (#{entity.id}, #{entity.amount}, #{entity.times}, #{entity.userId}, #{entity.address}, #{entity.hash}, #{entity.createTime})
            </foreach>
        </insert>

        <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
            insert into tb_bullfighting_purchase(id, amount, times, user_id, address, hash, create_time)
            values
            <foreach collection="entities" item="entity" separator=",">
                (#{entity.id}, #{entity.amount}, #{entity.times}, #{entity.userId}, #{entity.address}, #{entity.hash}, #{entity.createTime})
            </foreach>
            on duplicate key update
                id = values(id),
                amount = values(amount),
                times = values(times),
                user_id = values(user_id),
                address = values(address),
                hash = values(hash),
                create_time = values(create_time)
        </insert>

        <!--通过主键修改数据-->
        <update id="update">
            update tb_bullfighting_purchase
            <set>
                <if test="amount != null">
                    amount = #{amount},
                </if>
                <if test="times != null">
                    times = #{times},
                </if>
                <if test="userId != null and userId != ''">
                    user_id = #{userId},
                </if>
                <if test="address != null and address != ''">
                    address = #{address},
                </if>
                <if test="hash != null and hash != ''">
                    hash = #{hash},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime},
                </if>
            </set>
            where id = #{id}
        </update>

        <!--通过主键删除-->
        <delete id="deleteById">
            delete from tb_bullfighting_purchase where id = #{id}
        </delete>

    </mapper>
